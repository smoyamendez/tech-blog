<div class="card homepage-posts">
  <div class="card-body">
    <h5 class="card-title">{{title}}</h5>
    <h6 class="card-subtitle mb-2 text-muted">Posted by: {{user.name}} on {{format_date createdAt}}</h6>
    <p class="card-text">  {{post_content}}</p>
  </div>
  <a href="/post/{{id}}">View &amp; Comment</a>
</div>
